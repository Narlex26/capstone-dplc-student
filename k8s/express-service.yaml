apiVersion: v1
kind: Service
metadata:
  name: svc-express
  namespace: worldcup
spec:
  type: nodePort
  selector:
    app: express
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
    nodePort: 30007